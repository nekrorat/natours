doctype html
head
    block head
        //script(src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js" defer=true)
        //script(src='/js/mapbox.js' defer=true)
        //script(src='/js/login.js' defer=true)
        //script(src='/js/index.js' defer=true)
        script(src='/js/bundle.js', defer=true)
        script(src='https://js.stripe.com/v3' defer=true)
        meta(charset='UTF-8')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        link(rel='stylesheet' href='/css/style.css')
        link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
        link(rel='stylesheet' href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
        title Natours | #{title}
html
    body
        // HEADER
        include _header
        // CONTENT
        section.overview
        block content
            h1 This is a placeholder heading
        // FOOTER
        include _footer